package com.mantequilla.devplanner.utils

class StorageKey {
    companion object {
        const val userLoginInfo = "user_login_info"
        const val userEmail = "user_email"
        const val userId = "user_id"
    }
}